# Assignment 02 书面作答 / 填表题答题稿

> 整理需要书面作答、推导、现象记录、实验表格或性能归因的题，方便自己填写答案。
> 包含 `CONCEPT`、`DERIVE`、需要写报告的 `EXPERIMENT`、`DEBUG`、`MODIFY`、`FROM-SCRATCH` 题。
> 不包含只需要改代码、没有额外书面回答的纯代码题。
> 本文件只提供作答模板，不包含答案。

## Module 0 环境与峰值

### prob 0.1 HANDS-ON

文件：`cuda/m0_env/01_first_mma.cu`

运行记录：

| GPU | ARCH | 运行结果 / 现象 | 备注 |
| --- | --- | --- | --- |
| 5090 | `120a` |  |  |
| B300 | `100f` |  |  |
| 不匹配 ARCH |  |  |  |

解释不匹配 ARCH 编译/运行现象，并结合 fatbin/JIT 说明原因：

答案：

### prob 0.2 DERIVE

推导口径：

| 口径项 | 采用值 / 说明 |
| --- | --- |
| dense / sparse |  |
| boost / base 频率 |  |
| FMA 是否计作 2 FLOP |  |
| 其他假设 |  |

| 量 | 5090 | B300 |
| --- | --- | --- |
| bf16 FLOP/cycle/SM |  |  |
| bf16 峰值(TFLOPS) |  |  |
| fp8 峰值(TFLOPS) |  |  |
| fp4 峰值(TFLOPS) |  |  |
| datasheet 对照值与口径差异 |  |  |
| HBM/GDDR 带宽(GB/s) |  |  |
| 机器平衡点(FLOP/byte，bf16) |  |  |

计算过程：

答案：

与单条 mma 计算强度比较，并说明差距意味着什么：

答案：

为什么后续 M2--M4 需要从数据供给路径入手优化：

答案：

### prob 0.3 CONCEPT

判断下列说法是否正确，并给出一句理由。

| 小题 | 判断 | 理由 |
| --- | --- | --- |
| (a) 一条 mma 的计算强度，分子是 $2MNK$，分母按 A、B 读入与 D 写回的字节总和计。 |  |  |
| (b) mma.sync 是 warp 级协作指令，要求全 warp 一致执行；有 lane 发散时行为未定义。 |  |  |
| (c) 增大 mma 的形状 M/N/K 能提高单条指令计算强度，而且没有代价，所以指令形状越大越好。 |  |  |
| (d) 只要单条 mma 的计算强度低于机器平衡点，GEMM kernel 就不可能逼近计算峰值。 |  |  |

---

## Module 1 sm80: fragment 与 mma.sync

### prob 1.1 DERIVE

文件：`cuda/m1_sm80/01_fragment_map.cu`

A fragment 映射公式推导：

答案：

B fragment 映射公式推导：

答案：

附加问题：

1. A 的同一个 b32 寄存器中的 4 个 fp8 元素沿矩阵哪个方向相邻？

   答案：

2. 这个布局对 1.4 中使用 ldmatrix load 有什么影响？

   答案：

### prob 1.2 DEBUG

文件：`cuda/m1_sm80/02_bug_fragment.cu`

运行症状：

| 观察项 | 记录 |
| --- | --- |
| D 的哪些位置错 |  |
| 错成了什么 |  |
| 和正确部分的关系 |  |

修复说明：

答案：

解释错的是哪个 fragment 的哪部分映射，以及为什么产生上述症状：

答案：

### prob 1.4 MODIFY

文件：`cuda/m1_sm80/04_ldmatrix.cu`

`smem -> fragment` 阶段指令统计：

| 装载路径 | 装载指令数量 | 地址计算指令数量 | 备注 |
| --- | --- | --- | --- |
| 手工装载 |  |  |  |
| `ldmatrix` |  |  |  |

回答：

1. `ldmatrix` 省掉了手工装载中的哪些工作？

   答案：

2. 为什么这些工作在手工装载路径中无法避免？

   答案：

### prob 1.5 EXPERIMENT

文件：`cuda/m1_sm80/05_ldsm_stride.cu`

| 档位 | 预测 wavefront 比 | 实测 wavefront | 实测 conflict | 平均 cycle |
| --- | --- | --- | --- | --- |
| 32 B |  |  |  |  |
| 64 B |  |  |  |  |
| 128 B |  |  |  |  |
| 128+16 B |  |  |  |  |

比较预测与实测结果：

答案：

哪一种行跨度使 wavefront 数增加到 4 倍？

答案：

结合 8 个 warp 的占用情况，解释为什么 wavefront 增加 4 倍并不会使总耗时也增加 4 倍：

答案：

---

## Module 2 smem 供数：descriptor 与 swizzle

### prob 2.1 CONCEPT

(a) 将下面六个操作排成正确顺序，并说明每一步用于避免哪两个参与者之间的哪种乱序：

`wgmma.mma_async` / `st.shared` / `wgmma.commit_group` / `fence.proxy.async` / `wgmma.fence` / `wgmma.wait_group`

| 顺序 | 操作 | 避免的乱序 / 说明 |
| --- | --- | --- |
| 1 |  |  |
| 2 |  |  |
| 3 |  |  |
| 4 |  |  |
| 5 |  |  |
| 6 |  |  |

(b) 判断下列说法是否正确，并给出一句理由。

| 小题 | 判断 | 理由 |
| --- | --- | --- |
| 1. `fence.proxy.async` 是 wgmma 专属的指令，TMA 与 tcgen05 的场景不需要它。 |  |  |
| 2. `wgmma.commit_group` 会阻塞，直到它之前发射的 wgmma 全部完成。 |  |  |
| 3. 不加 `fence.proxy.async` 时，wgmma 可能读到 shared memory 中的旧值，因为 `st.shared` 的写经过 generic proxy，而 wgmma 的读经过 async proxy。 |  |  |

### prob 2.2 DERIVE

文件：`cuda/m2_smem/02_descriptor.cu`

| 场景 | LBO 推导 | SBO 推导 | layout 推导 | descriptor |
| --- | --- | --- | --- | --- |
| K-major，无 swizzle |  |  |  |  |
| K-major，128B swizzle |  |  |  |  |
| MN-major，128B swizzle |  |  |  |  |

场景 2 和场景 3 最终得到的 descriptor 相同。MN-major 与 K-major 的区别体现在哪里？

答案：

---

## Module 3 sm100：tcgen05

### prob 3.1 CONCEPT

判断下列说法是否正确，并给出一句理由。其中 (d) 写出计算过程。

| 小题 | 判断 | 理由 / 计算过程 |
| --- | --- | --- |
| (a) `tcgen05.ld` 读取 TMEM 时，每个 warp 只能读取自己对应的 32 条 lane，warp 之间不能互相读取。 |  |  |
| (b) 与 `mma.sync` 由 warp 协作、wgmma 由 warpgroup 协作不同，`tcgen05.mma` 由单个线程发射，随后由硬件异步执行。 |  |  |
| (c) TMEM 中的累加结果可以直接通过 TMA 搬回 global memory，不需要经过寄存器。 |  |  |
| (d) TMEM 每个 SM 包含 128 lane x 512 column x 4 B；一个 m128n256 的 f32 accumulator 恰好占用其中一半。 |  |  |
| (e) `tcgen05.commit` 会阻塞直到之前发射的 mma 全部完成，因此 commit 返回后即可安全读取 TMEM。 |  |  |

### prob 3.2 FROM-SCRATCH

文件：`cuda/m3_tcgen05/02_single_tile.cu`

正确版本判测记录：

答案：

去掉 `fence.proxy.async` 后的现象：

答案：

结合 2.1(a) 中的排序问题解释原因：

答案：

### prob 3.3 DEBUG

文件：`cuda/m3_tcgen05/03_bug_mbarrier.cu`

(a) 运行结果与问题出现条件：

| rounds | 运行结果 / 现象 | 备注 |
| --- | --- | --- |
| 1 |  |  |
| 2 |  |  |
| 4 |  |  |

问题出现的条件：

答案：

(b) mbarrier 状态变化：

修改前：

答案：

修改后：

答案：

错误版本在哪一次等待中过早或过晚放行：

答案：

为什么会产生 (a) 中观察到的现象：

答案：

### prob 3.4 EXPERIMENT

文件：`cuda/m3_tcgen05/04_cta_pair.cu`

(a) 预测与程序验证：

| 项目 | 预测 | 程序打印 / 实测 | 说明 |
| --- | --- | --- | --- |
| `cta_group::2` 每个 CTA 所需 B shared memory 相对 `cta_group::1` |  |  |  |
| TMEM 占用变化 |  |  |  |

(b) Nsight Compute shared memory 总流量：

| 实现 | shared memory 总流量 | 备注 |
| --- | --- | --- |
| `cta_group::1` |  |  |
| `cta_group::2` |  |  |

(c) `cta_group::2` 节省下来的 shared memory 容量，在 M4 的 pipeline 中可以用来做什么？

答案：

(d) `cta_group::2` 依赖 sm90 引入的哪一种硬件机制？5090 不支持 2-CTA MMA，结合 0.2 中整理的硬件参数，说明为什么这类机制更常出现在数据中心 GPU 上。

答案：

---

## Module 4 完整 GEMM

### 性能梯子总表

第 0 行需要在同一块 GPU 上重新运行 assignment01 Bonus 中的 naive matmul。

| 实现 | TFLOPS | 对 cuBLAS 达成率 | 一句话：时间主要花在哪 |
| --- | --- | --- | --- |
| naive（assignment01，fp32） |  |  |  |
| 4.1 tiled |  |  |  |
| 4.2 TMA |  |  |  |
| 4.3 pipeline（S=3） |  |  |  |
| cuBLAS |  | 100% |  |

### prob 4.1 FROM-SCRATCH

文件：`cuda/m4_gemm/01_tiled.cu`

判测与性能记录：

答案：

结合 0.2 中计算的机器平衡点，判断此时性能主要受哪一环节限制：

答案：

### prob 4.2 MODIFY

文件：`cuda/m4_gemm/02_tma.cu`

判测与性能记录：

答案：

比较 4.1 与 4.2：4.1 中 shared memory staging 的开销由哪些部分组成？

答案：

改用 TMA 后，其中哪些工作不再由普通 CUDA 指令完成？

答案：

Nsight Compute 辅助分析：4.1 中 SM 时间主要消耗在哪些部分？

答案：

### prob 4.3 FROM-SCRATCH

文件：`cuda/m4_gemm/03_pipeline.cu`

1. 使用 `S=3` 的结果填写性能表中 `4.3 pipeline` 一行。

记录：

答案：

2. 不同 stage 数性能测试：

| 形状 | S=2 | S=3 | S=4 | S=6 |
| --- | --- | --- | --- | --- |
| 4096^3 |  |  |  |  |
| 256 x 4096 x 16384 |  |  |  |  |

比较两个形状对 `STAGES` 的敏感程度，并结合 shared memory 用量、每个 SM 可同时驻留的 block 数以及 block 间并发能够隐藏的延迟进行解释：

答案：

3. 任选一个 `STAGES`，画出稳态阶段各 stage 中 TMA 与 mma 的流水时空图。

答案：

4. 回答：

(a) 从 4.1 到 4.3，主要瓶颈发生了哪些变化？

答案：

(b) 梯子表中每一级优化分别减少了哪部分开销？

答案：

(c) 如果继续增大 tile 或增加 stage 数，shared memory 与 TMEM 哪一个会先成为容量限制？结合 3.4(c) 的结果说明。

答案：

### prob 4.5 EXPERIMENT

文件：`cuda/m4_gemm/05_thin_gemm.cu`

峰值与带宽参数：

| 参数 | 数值 |
| --- | --- |
| Tensor Core 峰值 TFLOPS |  |
| 显存带宽 GB/s |  |

运行结果汇总：

| 投影层 / 形状 | M | N | K | AI | TFLOPS | GB/s | compute roof 达成率 | memory roof 达成率 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
|  | 1 |  |  |  |  |  |  |  |
|  | 8 |  |  |  |  |  |  |  |
|  | 16 |  |  |  |  |  |  |  |
|  | 64 |  |  |  |  |  |  |  |
|  | 256 |  |  |  |  |  |  |  |
|  | 1024 |  |  |  |  |  |  |  |
|  | 4096 |  |  |  |  |  |  |  |
|  | 16384 |  |  |  |  |  |  |  |
|  | 65536 |  |  |  |  |  |  |  |

(a) 描述性能随 M 变化的趋势。M 较小时，Tensor Core 性能从什么时候开始明显下降？M 增大到什么范围后进入相对稳定的平台？平台上的达成率是多少？

答案：

(b) 对性能下降的形状，比较 compute roof 和 memory roof 两种达成率。哪些形状主要受到显存带宽限制？

答案：

(c) `f_b_proj`（K=128）中两个 roof 的达成率都较低。结合矩阵形状分析，它的限制来自哪里？

答案：

(d) 根据上述结果解释 vLLM 在 $M \le 16$ 时选择 skinny CUDA Core kernel 的原因。

答案：

---

## Module 5 低精度与 block scaling

### prob 5.1 EXPERIMENT

文件：`kernels/quant_outlier.py`

| 采样点 $x\approx$ | 0.5 | 0.1 | 0.01 | 0.005 | 3000 |
| --- | --- | --- | --- | --- | --- |
| 相对误差 |  |  |  |  |  |

(a) 去掉 outlier 后重新量化，$x\approx0.5$ 处的误差变化多少倍？

答案：

(b) 找出输入被量化为 0 的阈值，并写出该阈值与 scale 的关系式。

答案：

(c) 改用 1x128 的 per-block scale 后，包含 outlier 的 block 与不包含 outlier 的 block 分别有什么变化？

答案：

### prob 5.2 DERIVE

文件：`kernels/block_scale_sim.py`

(a) 用两行代数式分别写出 row/column scale 为什么可以在整个点积外乘回，而 K-block scale 为什么必须逐段乘回。

答案：

(b) 结合 GEMM 内循环、连续供数和 Tensor Core 指令语义，说明硬件为什么把 block scale 与 K 归约对齐。

答案：

(c) DeepSeek-V3 的 weight 128x128 表示 scale 还在输出通道方向上共享，但每个组仍覆盖一段 K；NVFP4 则每 16 个 K 元素一组。结合 5.1(c) 的误差，说明粒度 16 相对粒度 128 有什么优势，又增加了多少 scale metadata 与供数复杂度。

答案：

### prob 5.3 FROM-SCRATCH

文件：`cuda/m5_lowprec/`

(c) Ceiling probe 记录：

| 项目 | 数值 |
| --- | --- |
| ceiling probe GB/s |  |
| quant kernel GB/s |  |
| 两者比值 |  |

结合 Nsight Compute 判断 quant kernel 距离自己的访存上限还有多远，以及剩余差距主要来自访存还是计算：

答案：

(d) Optional：使用 tcgen05 `kind::mxf4nvf4` 直接消费自己生成的 NVFP4 数据，SF 通过 TMEM 提供，并与 cuBLASLt 的结果对拍。

记录：

答案：

### prob 5.4 FROM-SCRATCH

文件：`cuda/m5_lowprec/04_fused_rms_nvfp4.cu`

逐形状结果：

| M | K | 两步基线 | 融合 kernel | 实测加速比 | 融合相对 5.3(c) ceiling probe 的性能比例 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 4096 |  |  |  |  |  |
|  | 7168 |  |  |  |  |  |
|  | 8192 |  |  |  |  |  |
|  |  |  |  |  |  |  |

解释实测结果与 2.56x 理论上限之间的差距：不同 M 范围内主要受到什么因素限制，并给出 Nsight Compute 数据或计算结果作为依据。

答案：

Optional：根据分析得到的主要瓶颈进行一次针对性优化，重新测试并更新表格。

记录：

答案：

### prob 5.5 CONCEPT

比较 W4A16 + Marlin kernel（权重 int4、计算 fp16）与 5.3 中的 NVFP4 GEMM。每问用两到三句话回答。

(a) 两者分别属于存储量化还是计算量化？

答案：

(b) 两种方法分别节省哪些资源：显存容量、显存带宽还是计算吞吐？

答案：

(c) 在 4.5 中的小 batch decode 场景下，哪一类量化的收益更加直接？

答案：

---

## Module 6 TileLang 对照

### prob 6.1 EXPERIMENT

使用 assignment01 的 `kernels/tilelang_matmul.py`，或任意使用 `T.gemm` 的 kernel，分别以 `sm_90a` 和 `sm_100a` 为 target 编译。

|  | sm_90a | sm_100a |
| --- | --- | --- |
| 选中的 Tensor Core 指令 |  |  |
| descriptor 在哪里、由谁生成 |  |  |
| smem swizzle 布局在哪一步确定 |  |  |
| 数据由谁搬入 smem |  |  |

(a) 哪些硬件相关的决策已经由 DSL 自动完成？

答案：

(b) 哪些参数仍然需要程序员决定，例如 tile 尺寸和 stages？

答案：

在 assignment01 7.5 的“谁负责”表中补充一行：

| 问题 | 手写 CUDA | TileLang / DSL |
| --- | --- | --- |
| Tensor Core 指令选择与供数布局 |  |  |

---

## 团队选做

团队题不限制 AI 工具的使用。若完成 C1/C2，在此记录报告材料入口。

| 题目 | 复现与测量 | 分析结论 + 证据 | 挑战阶段记录 |
| --- | --- | --- | --- |
| C1 FlashKDA |  |  |  |
| C2 MSA decode |  |  |  |
